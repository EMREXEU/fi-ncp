# Local Schema definitions for IDEs.
Local schemas are defined at `resources/xsd`.
## IntelliJ IDEA configuration
>Settings → Languages & Frameworks → Schemas and DTDs
# Schema Catalog
Defined at `resources/xjc/catalog.xml`.